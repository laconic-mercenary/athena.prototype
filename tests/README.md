# Tests

Unit tests live here (pytest). Run with `pytest` from the project root.

System integration tests are in `system/` — each subdirectory is a self-contained
scenario with its own Docker stack.
